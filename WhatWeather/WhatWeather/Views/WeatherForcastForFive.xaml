<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:WhatWeather.UserControl"
             x:Class="WhatWeather.WeatherForcastForFive" Title="5 Dayes">
    <ContentPage.Content>
        <Grid VerticalOptions="Fill">
            <Grid.RowDefinitions>
                <RowDefinition Height="40" />
                <RowDefinition Height="*"  />
            </Grid.RowDefinitions>
            <ContentView BackgroundColor="{StaticResource BrandAccentColor}">
                <Label Text="5 day / 3 hour forecast" HorizontalOptions="StartAndExpand" VerticalOptions="Center" Font="{StaticResource SubheadFont}" TextColor="{StaticResource SecondaryTextColorOnDark}" />
            </ContentView>
            <ContentView Grid.Row="1"  BackgroundColor="{StaticResource BrandColorLight}" >
                <local:LVWeatherForecast x:Name="list" IsVisible="True" ItemsSource="{Binding Climate}" />
            </ContentView>
        </Grid>
    </ContentPage.Content>
</ContentPage>